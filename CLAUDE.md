# CLAUDE.md

Context for AI-assisted development on `lazy-cherry-pick`. Read this first when entering the repo.

## In one paragraph

A Tauri 2 desktop app for batch Git cherry-pick workflows. Rust backend spawns a Go sidecar and talks to it over **newline-delimited JSON-RPC 2.0 on stdin/stdout**. The sidecar shells directly to `git` CLI (no library). Frontend is Svelte 5 + TypeScript via SvelteKit. M15a done (now expanded): the same sidecar binary doubles as a hand-rolled **MCP server** (`--mcp` flag, zero impact on the desktop app) exposing **22 git tools** to AI clients — read (status, branches, commits, diffs, remotes, conflict inspection), write (cherry-pick, fetch/pull/push, create branch, conflict resolution), and an AI-driven conflict-resolution loop (`cherry_pick` → `needs_human_resolution` → `get_conflict_content`/`resolve_conflict_file`/`resolve_conflict_side`/`continue_cherry_pick`) — see `docs/MCP.md`. Forge (PR/MR) tools remain M15b. M14c done: Bitbucket Cloud support added (Atlassian API Token + Basic Auth, App Password deprecation 2026-07-28); CreatePR dialog polished — required-field asterisk, auto-fill title from latest commit even on "+" direct flow. M14b done: PR status bar auto-discovery, hover popover, "+" direct-create button, find-existing-PR. M14a done: GitHub + GitLab PR/MR creation MVP — OS keychain, per-repo connection. M13 done: multi-remote push dropdown + Copy SHA + Open commit. M10 done: keyboard nav, drag-drop, undo, Toast. M9 done: auto-updater. M8 done: external diff/merge tools. All UI text in English.

## File map

| Path | Role |
|---|---|
| `app/src/routes/+page.svelte` | Main orchestrator — 3-pane UI, all app state |
| `app/src/routes/diff/+page.svelte` | File diff viewer window (`?staged=true` switches to staged diff via `git.stagedFileDiff`) |
| `app/src/routes/conflict/+page.svelte` | 3-pane conflict merge editor (Theirs / Ours / merged result). **Interactive** variant (drag-select, context menu, provisional choices, Save & Stage). Since M16e, parse/render logic comes from `$lib/conflict-parse` (shared with `ConflictThreePane`) |
| `app/src/lib/Toolbar.svelte` | Repo path display + Open repo (file dialog) |
| `app/src/lib/CommitList.svelte` | Source branch dropdown + scrollable commit list with checkboxes |
| `app/src/lib/PickQueue.svelte` | Ordered pick queue + target branch dropdown + Apply button. M11b: "Squash into one" checkbox + squash-message textarea; per-row ✎ inline commit-message override |
| `app/src/lib/ResultBanner.svelte` | Result display: ✅ applied / ⚠️ conflict / 🔴 error |
| `app/src/lib/CommitDetail.svelte` | Commit subject, body, SHA, author, date, parents panel |
| `app/src/lib/CommitFiles.svelte` | File list for a commit with status badge (+/- stats), click to open diff. M11c: per-file checkboxes + "Pick N files → \<target\>" footer for partial-file pick |
| `app/src/lib/FileDiff.svelte` | 2-panel side-by-side diff renderer (TortoiseGit-style): sync scroll, EOL marker toggle (¶), trailing-newline transform |
| `app/src/lib/ConflictResolver.svelte` | Conflict file list — Keep Ours / Use Theirs / Continue / Abort; resolved files open staged diff; M16 "🤖 AI resolve all" button + per-file Review/Accept/Discard for AI-pending files |
| `app/src/lib/AiReviewModal.svelte` | M16 — review modal for AI-resolved conflicts. Thin shell: **Diff/3-way toggle** (M16c, default "3-way"); "3-way" renders `<ConflictThreePane>`; "Diff" = `FileDiff` side-by-side ("Conflict original" vs "AI resolved"). Footer Accept & stage / Discard apply regardless of view |
| `app/src/lib/ConflictThreePane.svelte` | **M16e** — read-only 3-pane conflict viewer (Theirs/Ours top + merged-result bottom), pixel-matching the manual editor's layout/colors/conflict-header bars/fillers/H-divider/nav. Props: `conflictText` (markers) + `mergedText` (resolved) + labels. Uses `conflict-parse`. Reusable by any future read-only conflict feature |
| `app/src/lib/conflict-parse.ts` | **M16e** — shared pure functions `parseConflict()` / `buildRenderLines()` / `normalizeConflictText()` + types (`Part`, `RenderLine`, `Rendered`). Single source of truth for conflict-marker parsing, used by BOTH the manual editor (`routes/conflict/+page.svelte`) and `ConflictThreePane.svelte` |
| `app/src/lib/ai-providers.ts` | M16b — AI provider presets (Claude/Gemini/Codex/Aider/Custom) as data + `renderAiArgs()` (template → argv, `{model}`/`{prompt}` placeholders). Feeds the generic `run_ai_resolve` Rust command |
| `app/src/lib/Settings.svelte` | Settings modal — **vertical sidebar tabs** (660px, `activeTab` state): General (maxCommits, defaultApplyMode, showEolMarkers, autoFetchOnOpen, **autoStash** (M11a), theme, checkForUpdatesOnStartup/"Check Now"), AI Resolution (M16/M16b), External Tools (diff/merge + reference table), Accounts (**M14f** — lists ALL connected repos with per-card Test/Reconnect/Disconnect + "connect a recent repo" dropdown) |
| `app/src/lib/UpdateBanner.svelte` | Update notification banner — shows version, download progress bar, "Update Now" / "Later" buttons |
| `app/src/lib/Toast.svelte` | Toast notification system — fixed bottom-right, auto-dismiss for success/warning, manual dismiss for errors |
| `app/src/lib/GitConsole.svelte` | Git Console panel — realtime git command log grouped by RPC call; load history from file; Clear button. M16d: also renders 🤖 AI CLI invocation entries (`type: "ai"`) alongside git cmd/info entries |
| `app/src/lib/rpc.ts` | Typed wrapper around `invoke('sidecar_call', ...)` + direct Tauri commands |
| `app/src/lib/rpc-types.ts` | TypeScript types mirroring Go sidecar types |
| `app/src-tauri/src/lib.rs` | Rust entry; `sidecar_call` (stderr `[GIT_CMD]`/`[GIT_INFO]` parsing + git-log events + file append), `settings_load/save`, `git_log_read/clear`, `recents_load/save`, `launch_detached`, `launch_and_wait`, `detect_external_tools`, `detect_ai_tool`/`run_ai_resolve` (M16b generic AI CLI engine; M16d wraps it with `log_ai_result` → same `git.log`/`git-log` stream as `type: "ai"`) |
| `app/src-tauri/Cargo.toml` | `tauri-plugin-shell`, `tauri-plugin-dialog`, `tauri-plugin-updater`, `tauri-plugin-process`, `tokio` (features: `sync`, `io-util`, `macros`, `process`) |
| `sidecar/internal/git/externaltool.go` | `ExtractDiffFiles`, `ExtractConflictFiles`, `StageResolvedFile`, `CleanupTmpDir` — extracts file versions to `lcp-diff-*`/`lcp-merge-*` temp dirs for external tools |
| `app/src-tauri/tauri.conf.json` | `bundle.externalBin: ["binaries/sidecar"]` |
| `app/src-tauri/capabilities/default.json` | `shell:allow-*` for sidecar + `dialog:allow-open`; window globs: `diff-*`, `conflict-*` |
| `app/src-tauri/binaries/sidecar-<triple>.exe` | Built sidecar binary — Tauri requires the target triple suffix |
| `sidecar/main.go` | NDJSON JSON-RPC dispatcher; `wrap1` generic helper; `toRPCError` bridge; `git.cherryPick` uses manual handler to inject progress callback; `--mcp` flag branches to `internal/mcp` before any of this |
| `sidecar/internal/rpc/server.go` | NDJSON JSON-RPC 2.0 transport; BOM stripping; dispatch loop; `ProgressWriter` + `WriteProgress` |
| `sidecar/internal/mcp/` | MCP server (M15a) — `server.go` (protocol/transport), `tools.go` (22 tools → `internal/git`), `server_test.go`. Started via `sidecar.exe --mcp`. See `docs/MCP.md` |
| `sidecar/internal/git/` | Git operations layer (24 files) — see sidecar/README.md |
| `sidecar/go.mod` | Module `github.com/lazy-cherry-pick/sidecar`; Go 1.23 |
| `docs/IPC.md` | Full protocol spec — method signatures, types, error codes (Tauri ↔ sidecar NDJSON) |
| `docs/MCP.md` | MCP server protocol, 22-tool catalog, conflict-resolution loop, client config (`--mcp`, `npx`) |
| `mcp-npm/` | npm wrapper package `lazy-cherry-pick-mcp` — `bin/cli.js` downloads the matching `sidecar-x86_64-pc-windows-msvc.exe` from GitHub Releases on first run, caches under `%LOCALAPPDATA%`, execs with `--mcp`. Zero deps. Version must match the release tag (`vX.Y.Z`) — bump alongside `app/package.json`/`Cargo.toml`/`tauri.conf.json` |

## Commands

Fresh PowerShell sessions on Windows often lack cargo/go on PATH. Always prepend:
```powershell
$env:Path = "$env:USERPROFILE\.cargo\bin;C:\Program Files\Go\bin;" + $env:Path
```

**Rebuild sidecar** (required after editing any `sidecar/*.go`):
```powershell
$triple = (rustc -vV | Select-String "host:").ToString().Split(":")[1].Trim()
Set-Location sidecar
go build -o ..\app\src-tauri\binaries\sidecar-$triple.exe .
```

**Run Go integration tests:**
```powershell
Set-Location sidecar
go test ./internal/git/ -v
```

**Sidecar smoke test** (no Tauri — verify Go layer in isolation):
```powershell
$s = ".\app\src-tauri\binaries\sidecar-x86_64-pc-windows-msvc.exe"
'{"jsonrpc":"2.0","id":1,"method":"ping"}' | & $s
'{"jsonrpc":"2.0","id":2,"method":"git.status","params":{"repo":"C:\\path\\to\\repo"}}' | & $s
```

**Rust compile check** (no Vite needed):
```powershell
Set-Location app\src-tauri
cargo build
```

**Dev server (full app)** — must be run in the user's own terminal (see hard rule #1):
```powershell
.\dev.ps1
# or: cd app && npm run tauri dev
```

## Hard rules for AI agents

1. **Never run `npm run tauri dev` via a background/agent task.** Gets terminated when parent shell exits. Always ask the user to run it in their own terminal. (Verified during M1.)

2. **After editing `sidecar/*.go`, rebuild the binary to `app/src-tauri/binaries/sidecar-<triple>.exe`.** Tauri bundles the binary at dev time — source changes are invisible until rebuild. On this machine the triple is `x86_64-pc-windows-msvc`.

3. **When adding RPC methods:** (a) implement in `sidecar/internal/git/`, (b) register in `sidecar/main.go` via `s.Register("name", wrap1(...))` — except methods that need progress streaming, which use a manual handler (see `git.cherryPick` pattern), (c) update `docs/IPC.md`, (d) add TypeScript types to `rpc-types.ts`, (e) add typed wrapper to `rpc.ts`. No Rust change needed per method.

4. **JSON over stdio is NDJSON** — one JSON object per line. Never embed raw `\n` inside a JSON value. Go's `json.Encoder.Encode` always appends `\n` — keep using it.

5. **Strip UTF-8 BOM on every input line.** `rpc/server.go` does `bytes.TrimPrefix(line, utf8BOM)`. Don't remove — Windows PowerShell pipes prepend BOM.

6. **Target branch model is Model B with default = current HEAD.** Sidecar always checks dirty tree before applying. If `target != current`, checkout happens only after dirty-tree check passes. Implemented and tested in `cherrypick.go`. **M11a auto-stash does NOT relax this** — the frontend stashes (`git.stash`) *before* calling `cherryPick`, so the tree is already clean when the check runs; the rule and `cherrypick.go` are unchanged.

7. **The sidecar is the only layer that shells out to `git`.** No `git` calls in Rust, no direct `git_*` invocations from Svelte. Path: Frontend → `rpc.ts` → `invoke('sidecar_call')` → Rust `sidecar_call` → Go sidecar → `git`.

8. **The frontend has no `@tauri-apps/plugin-shell` dependency.** Svelte uses `@tauri-apps/plugin-dialog` (for folder picker) and our own `sidecar_call` command only. Never add `plugin-shell` to Svelte — it would expose shell APIs to the WebView.

## Architecture invariants

- **One sidecar process per RPC call (current model).** `sidecar_call` does `spawn → write 1 line → read N lines → kill`. Progress-streaming calls (`git.cherryPick`) emit multiple `progress` lines before the final `result` line. Rust reads in a loop, emits Tauri event `cp-progress` for each progress line, then resolves on the final line. ~30–50 ms overhead per call.
- **Concurrent sidecar calls are safe.** `ActiveSidecar` is `Mutex<HashMap<u64, CommandChild>>` with a per-call atomic `call_id`. Each call stores its child by `call_id` and removes only its own entry on finish — concurrent calls from multiple windows (e.g. main + conflict editor) cannot kill each other's processes. `sidecar_cancel` drains the entire map.
- **Progress protocol:** intermediate lines carry `"progress"` field (not `"result"/"error"`) with the same `id`. Rust detects by `parsed.get("progress").is_some()`. Frontend listens via `@tauri-apps/api/event` `listen("cp-progress", ...)`.
- **Cancel:** `sidecar_cancel` Tauri command drains `ActiveSidecar` (`Mutex<HashMap<u64, CommandChild>>`), killing all active children. Frontend then calls `git.abort` via a new sidecar spawn to run `git cherry-pick --abort`.
- **Recent repos:** stored in `%APPDATA%/com.lazycherrypick.app/recents.json` by Rust commands `recents_load`/`recents_save`. Max 10 entries. Not routed through the Go sidecar.
- **App settings:** stored in `%APPDATA%/com.lazycherrypick.app/settings.json` by Rust `settings_load`/`settings_save`. Fields: `maxCommits`, `defaultApplyMode`, `showEolMarkers`, `autoFetchOnOpen`, `autoStash` (M11a), `theme`, `externalDiffEnabled`, `externalDiffPath`, `externalDiffArgs`, `externalMergeEnabled`, `externalMergePath`, `externalMergeArgs`, plus `checkForUpdatesOnStartup`, the `ai*` fields (M16b), and `forgeConnections` (M14). Returns defaults on missing/corrupted file. All fields have `#[serde(default)]` — backward compatible.
- **External tool launch:** `launch_detached(program, args)` — fire-and-forget for diff; `launch_and_wait(program, args)` — async block until tool exits for merge. Both are Rust Tauri commands using `tokio::process::Command`. `detect_external_tools()` checks 5 known paths (TortoiseGit, BC3, BC4, WinMerge, VSCode) and returns found tools as `[{name, path}]`.
- **Git command log:** sidecar emits `[GIT_CMD] git <args>` (without `-C dir`) and `[GIT_INFO] git.method → branch` to stderr. Rust batchs stdout (RPC protocol) and stderr separately — stderr lines with those prefixes are emitted as Tauri `git-log` events and appended to `%APPDATA%/com.lazycherrypick.app/git.log`. Format per line: `<type> <unix-ts> <content>` where type is `cmd` or `info`.
- **Sidecar logs to stderr, responses to stdout.** Don't write logs to stdout.
- **Capabilities are scoped to `binaries/sidecar`.** Don't broaden to `shell:default`.
- **Write operations require explicit user action.** `git.cherryPick` is only triggered by the Apply button — never called automatically. Read methods (`git.status`, `git.commits`, `git.fetch`) are fine to call on load/branch-change.

## What's done / what's not

**M1 done**: project scaffold, IPC contract validated end-to-end (`ping`, `version`).

**M2 done**: full Go git ops layer — `git.openRepo`, `git.status`, `git.branches`, `git.commits`, `git.cherryPick`. 9 integration tests (all pass). TypeScript typed wrapper (`rpc.ts`, `rpc-types.ts`).

**M3 done**: main UI — Toolbar (file dialog open), CommitList (source branch + multi-select), PickQueue (ordered queue + target dropdown + Apply), ResultBanner (applied/conflict/error). Happy path fully wired end-to-end.

**M3.1 done**: Apply & Push — TortoiseGit-style dropdown on Apply button. `git.push` RPC method. Mode state persists between operations.

**M4 done**:
- **Progress streaming**: `git.cherryPick` emits progress lines (`{"progress": {"n":1,"total":3,"sha":"..."}}`) before final result. Rust reads in loop + emits Tauri event `cp-progress`. `PickQueue` shows progress bar `n/total — sha`.
- **Cancel**: `sidecar_cancel` Tauri command kills current sidecar child. Frontend calls `git.abort` to cleanup git state. Cancel button visible during apply.
- **Recent repos (M4b)**: Rust `recents_load`/`recents_save` commands write `%APPDATA%/lazy-cherry-pick/recents.json`. Toolbar shows "Recent ▾" dropdown (max 10 entries).
- **Fetch / Pull refresh**: `git.fetch` (`git fetch --prune`) and `git.pull` (`git fetch <remote> <branch>:<branch>`, fast-forward only). CommitList header has Fetch/Pull dropdown button.

**M5 mostly done**:
- **M5a — Commit detail panels**: click commit → resizable panel below shows CommitDetail (subject, body, SHA, author, date, parents) + CommitFiles (file list with status badge, +/- stats, click to open diff window). Horizontal resize handle between CommitList and PickQueue panes.
- **M5b — Dry-run conflict preview**: `git.dryRunPick` RPC uses `git apply --3way --check`; ⚠ icon on queue items with predicted conflicts; debounce 400 ms.
- **File diff viewer**: `git.fileDiff` RPC (`git show --unified=99999`); opens in a new Tauri window (`diff-${Date.now()}`); nav buttons ▲/▼ to jump between change blocks. Now **2-panel side-by-side (M5d)** — see below.
- **Create Branch**: `git.createBranch` RPC; Create Branch button in PickQueue target dropdown.
- **M5c — Conflict resolver (done)**: `git.conflictFiles`, `git.resolveConflict`, `git.continueCherry`, `git.fileContent`, `git.writeAndStageFile` RPCs; `ConflictResolver.svelte` with Keep Ours/Use Theirs/Continue/Abort; `/conflict` route — TortoiseGit-style 3-pane merge editor:
  - **Line numbers** in each top pane (independent theirs/ours counters)
  - **Inline conflict-header bar** per block: "Conflict N/M · ← Theirs · T+O" (left) / "Ours → · O+T" (right) — action buttons right at the conflict, no need to use toolbar
  - **Independent drag-select per pane**: `leftSel` + `rightSel` state — selecting on one pane does NOT clear the other; right-click shows cross-pane combine options when both have selections
  - **Context menu**: use selected lines from this pane / use whole block / combine both sides (4 order combinations)
  - **Resizable H-divider**: drag to resize top/bottom ratio (20–80%)
  - **Keyboard**: ↑↓ to navigate conflicts
  - **Provisional resolution model**: clicking Theirs/Ours/T+O sets a *soft choice* in `provisionalChoices: Map<number, string[]>` — `mergedText` keeps conflict markers intact. Bottom pane shows chosen lines in **orange** immediately, but user can click any button again to change. `buildFinalText()` applies all choices only at save time.
  - **Bottom pane (TortoiseGit style)**: provisionally-resolved blocks shown in orange with change buttons (← Theirs / Ours → / T+O) always visible; unresolved blocks shown as **hatched red placeholder** with Ours/Theirs/T+O buttons; click block → jumps top panes to that conflict
  - **Toggle "✎ Raw"**: enables inline contenteditable on `.mv-text` spans (visual mode + direct edit). Yellow highlight on edited lines. Exit syncs DOM back only if `rawEdited` was set (avoids overwriting state when user just looked at raw mode).
  - **Save status**: button "Lưu & Stage" disabled when `hasUnresolved || applying || saved`; after save shows "✓ Đã lưu" badge instead of auto-closing window.
  - Flow: cherry-pick stops in conflict state → ConflictResolver shown → click file → conflict window opens → resolve each block (changeable) → Lưu & Stage → click Continue → `continueCherry` applies the staged commit + remaining queue commits via `applyPickShas()`.

- **M5d — Polish & diff viewer redesign (done)**:
  - **Project rename**: `cherry-pick-git` → `Lazy Cherry Pick`. Tauri productName/identifier updated; AppData path is now `lazy-cherry-pick/recents.json`; Go module is `github.com/lazy-cherry-pick/sidecar`.
  - **Staged diff for resolved files**: `git.stagedFileDiff` RPC (`git diff --cached --unified=99999 -- <file>`); ConflictResolver opens this in a diff window (gray button) when a file is already resolved, instead of the merge editor.
  - **2-panel side-by-side diff viewer (TortoiseGit-style)**: complete rewrite of `FileDiff.svelte`. Layout: left/right panels with independent labels (`Before`/`{sha}` for commit diff, `HEAD`/`Staged` for staged diff), synced scroll, hatched filler rows when one side has no matching line, hunk header spans both panels.
  - **EOL markers (Notepad++ style)**: toggle `¶` button shows `LF` (blue) / `CRLF` (orange) at end of each line. Parser auto-detects trailing `\r` and handles `\ No newline at end of file` as `eol="none"` (no marker rendered).
  - **Trailing-newline transform (Option D + lookahead)**: when a paired `change` row has `leftText === rightText` but EOL differs ("none" vs "lf"/"crlf"), convert to `ctx` row + phantom empty `change` row (representing the implicit empty line that appears/disappears with the trailing newline). Lookahead skips the phantom if the next row is already an empty del/add — prevents double-counting when the diff covers both an empty line AND the trailing newline. Matches TortoiseGit's "editor view" rather than git's "lost trailing newline" view. See `design-side-by-side-diff` memory.
  - **Raw-mode save fix in conflict editor**: setting `rawEdited = false` after successful save prevents the "blank screen" bug when toggling back to visual mode.

**M6 done**:
- **Checkbox hit area fix**: Wrapped `<input type="checkbox">` in `<label class="cb-wrap">` with extra padding — click area ~36×36px instead of 14×14px. Row `align-items` changed to `center`.
- **BranchSelect component** (`app/src/lib/BranchSelect.svelte`): Custom searchable dropdown replacing native `<select>` in both CommitList (source) and PickQueue (target). Type to filter by name; ↑↓/Enter/Escape keyboard nav; click-outside closes.
- **M6a — Commit filter bar**: Collapsible filter bar in CommitList header. Fields: Author, Message (keyword/regex), Since, Until, Path (glob). `Filter (N)` badge button toggles bar; Apply/Clear; Enter key submits. Filter state held in `+page.svelte` as `activeFilter: CommitFilter`; passed to `rpc.git.commits(..., filter)`; resets on source branch change.
- **M6b — Filter presets**: Save/load named filter presets stored in `localStorage` key `lcp-filter-presets`. "+ Save" button → inline name input → Enter to save. "Presets (N) ▾" dropdown lists saved presets; click to load; trash icon to delete. Persists across app restarts. **Active preset UX** (post-M6 polish): Presets button shows active preset name (highlighted accent color) instead of count; when preset is loaded and filter fields are modified, "+ Save" becomes a "Save | ▾" split-button — left click overwrites the active preset, ▾ opens dropdown with "Save [name]" (overwrite) and "Save as…" (new name input). State: `activePresetName: string | null`, `filtersDirtyFromPreset: boolean` (derived).
- **M6c — Commit badges**: `getBadge(subject)` in CommitList detects conventional commit prefix (`feat|fix|docs|style|refactor|perf|test|build|ci|chore`) and JIRA pattern (`[ABC-123]`); strips prefix from displayed subject. Colors: feat=green, fix=red, docs=blue, test=purple, perf=yellow, chore/…=gray, breaking `!`=dark red, JIRA=light blue. Uses `{@const badge = getBadge(c.subject)}` at `{#each}` level.
- **Bug fix — root commit loading**: Root commit `nil` parents slice → JSON `null` → `detail.parents.length` TypeError in Svelte → component stuck at "Loading...". Fix: Go initializes `parents := []string{}`; frontend adds `(detail.parents ?? [])` guard in `CommitDetail.svelte`.
- **Build & CI fixes**: `build/build.ps1` — fixed project name in comment, added PATH setup, added `go test` gate. `releases/*.md` — fixed path `.\build.ps1` → `.\build\build.ps1`. `.github/workflows/release.yml` — Go `1.21`→`1.23`, Node `lts/*`→`'22'`, `tauri-action@v0`→`v0.5`, added `go test` step.
- **gitignore**: Added `app/src-tauri/binaries/*.exe` — sidecar binary is a build artifact, not tracked in git.
- **TestCherryPick_Conflict updated**: Test was written for M4 behavior (auto-abort on conflict). M5 leaves repo in conflict state. Test now asserts dirty after conflict → calls `Abort()` → asserts clean.

**M7 done**:
- **M7a — Settings panel**: `AppSettings` struct in Rust (`maxCommits`, `defaultApplyMode`, `showEolMarkers`, `autoFetchOnOpen`, `theme`). `settings_load`/`settings_save` Tauri commands write `%APPDATA%/com.lazycherrypick.app/settings.json`; returns defaults on missing/corrupted file. `Settings.svelte` modal with number input, select, toggle switches, Dark/Light segmented control. Gear button in `Toolbar.svelte`. `+page.svelte` applies settings: `$effect` toggles `body.light` class; `loadCommits` uses `settings.maxCommits`; `openRepo` calls `git.fetch` if `autoFetchOnOpen`; PickQueue uses `defaultApplyMode`; FileDiff uses `initialShowEol`.
- **Theme (dark/light)**: CSS custom properties (`--surface`, `--surface-elevated`, `--border`, `--toolbar-bg`, `--input-bg`, `--hover`, `--selected`, `--accent`, `--text`, `--text-secondary`, `--text-muted`) defined on `:global(body)` for dark; `body.light` block overrides all. Sub-windows (`diff/`, `conflict/`) load settings independently in `onMount` and apply class to `document.body`. Conflict editor panes kept dark regardless of theme (code readability). `var(--surface-elevated)` used in all dropdown menus across components.
- **English UI**: All Vietnamese text translated to English across all `.svelte` files.
- **Git Console**: `GitConsole.svelte` panel (180px, always dark). Toggle button (`>_`) in Toolbar with active state. Go `exec.go` logs `[GIT_CMD] git <args>` (without `-C dir`) to stderr before each git exec. `server.go` logs `[GIT_INFO] git.method → target/branch` to stderr before each `git.*` dispatch. Rust parses both prefixes from `CommandEvent::Stderr`, emits `git-log` event `{ts, type, cmd}`, appends `<type> <ts> <cmd>` to `git.log`. Frontend loads history via `git_log_read` on mount, listens `git-log` for realtime. Renders `info` entries as group headers (method + branch), `cmd` entries as indented lines with `git` (dim) + subcommand (cyan bold) + args (gray). Auto-scroll with pause when user scrolls up. `git_log_clear` Tauri command truncates the file.

**M8 done**:
- **External diff viewer**: Settings → External Tools → enable + set path/args → clicking a file in CommitFiles now calls `git.extractDiffFiles` (Go sidecar extracts `SHA^:file` and `SHA:file` to `lcp-diff-*` temp dir), builds CLI args from template (`{left}`, `{right}`, `{leftLabel}`, `{rightLabel}`), calls `launch_detached` Rust command. Temp files are NOT cleaned — OS cleans on reboot.
- **External merge tool**: Settings → External Tools → enable + set merge path/args → clicking a conflict file calls `git.extractConflictFiles` (extracts `:1:` base, `:2:` ours, `:3:` theirs, working-tree copy as output to `lcp-merge-*` temp dir), calls `launch_and_wait` (blocks until tool closes), then `git.stageResolvedFile` writes output back + stages it, then `git.cleanupTmpDir` removes temp dir.
- **Args template**: `buildArgs()` helper in `+page.svelte` substitutes `{key}` placeholders then splits respecting quoted tokens. Critical: regex uses `([^\s"]*)` (NOT `\S*`) as unquoted prefix — `\S` includes `"` causing backtracking that embeds `"` chars in args → Rust's CreateProcessW escaping corrupts paths (`D:\C:\...` prefix, `\` suffix).
- **Conflict marker guard**: `StageResolvedFile` in `externaltool.go` checks `bytes.Contains(data, []byte("<<<<<<<"))` before staging — prevents auto-staging when merge tool is closed without resolving all conflicts.
- **TortoiseGit tool split**: `TortoiseGitProc.exe` for diff; `TortoiseGitMerge.exe` (same `bin/` dir) for 3-way merge. Auto-detect fills merge path via string replace. `/command:merge` on TortoiseGitProc.exe opens wrong dialog.
- **TortoiseGit diff direction**: `/path:` arg → RIGHT pane; `/path2:` arg → LEFT pane (opposite of naming). Correct: `/command:diff /path2:"{left}" /path:"{right}"`.
- **Auto-detect auto-fills args**: `applyDetected()` in `Settings.svelte` fills `TOOL_DIFF_ARGS[name]` / `TOOL_MERGE_ARGS[name]` constants when user clicks a detected tool pill.
- **Browse button**: `@tauri-apps/plugin-dialog` `open()` with exe filter — lets user pick .exe without typing path.
- **FileDiff.svelte status-aware empty messages**: status "A" → "File did not exist" (left) / "New empty file" (right); status "D" → "Empty file" / "File was deleted". Fixes "No changes." shown for newly-added empty files.
- **conflict/+page.svelte async onMount fix**: wrapped async init in IIFE, returned sync cleanup — fixes TypeScript error from Svelte's `onMount` typing.

**M9 done**:
- **Auto-updater**: `@tauri-apps/plugin-updater` + `@tauri-apps/plugin-process` registered in Rust. `check()` called on startup if `checkForUpdatesOnStartup: true`. `UpdateBanner.svelte` shows version + progress bar. `installUpdate()` has try/catch to reset state on error. `relaunch()` called after install.
- **Settings**: `checkForUpdatesOnStartup` toggle + "Check Now" button (`onchecknow` prop). `AppSettings` struct in Rust adds `check_for_updates_on_startup: bool` with `default_true()`.
- **Updater endpoint**: `https://github.com/tentaikhoan24/lazy-cherry-pick/releases/latest/download/latest.json`. Public key in `tauri.conf.json` `plugins.updater.pubkey`.
- **CI signing**: `TAURI_SIGNING_PRIVATE_KEY` GitHub secret (ed25519 key generated via `cargo tauri signer generate`). `bundle.createUpdaterArtifacts: true` in `tauri.conf.json`. CI generates `latest.json` + `.sig` files automatically on tag push.
- **Capabilities**: `updater:allow-check`, `updater:allow-download-and-install`, `process:allow-restart` added to `default.json`.
- **Key pitfalls**: correct function is `relaunch()` not `restart()`; `Progress` event only has `chunkLength`, `contentLength` is in `Started` event; `TAURI_SIGNING_PRIVATE_KEY_PASSWORD` must NOT be set as GitHub secret if key has no password (resolves to empty string automatically).

**M10 done**:
- **CommitList keyboard nav**: ↑/↓ moves focus between rows (using `bind:this` + `.focus()`), Space toggles checkbox, Enter selects commit (opens detail panel). `:focus-visible` outline on rows.
- **PickQueue keyboard**: ↑/↓ moves focus, Delete/Backspace removes focused item, tabindex="0" on `<li>` items.
- **Drag-drop reorder PickQueue**: HTML5 drag-drop (`draggable="true"`, `ondragstart/ondragover/ondrop/ondragend`). `onreorder(from, to)` prop → `reorderQueue()` in `+page.svelte` splices `selectionMap` values array and rebuilds Map. `.drag-over` CSS indicator (dashed top border).
- **Undo stack (Ctrl+Z)**: `undoStack: Map<string, Commit>[]` in `+page.svelte`. `pushUndo()` called before `toggleCommit`, `removeFromQueue`, `reorderQueue`. `undo()` pops last snapshot. Stack cleared on successful apply. MAX_UNDO = 20.
- **Global shortcuts**: `<svelte:window onkeydown={handleGlobalKey}>`. Ctrl+Enter triggers `applyPick(false)` when queue not empty and not busy. Ctrl+Z triggers `undo()`. Both skip when focus is in an input/textarea/contenteditable.
- **Toast notifications** (`Toast.svelte`): fixed bottom-right, slide-in animation, auto-dismiss (5s success, 8s warning, error requires manual dismiss). `showToast(type, message, detail?)` replaces `applyError`/`applyResult` for non-conflict flows. ResultBanner now only shows for conflict state (`applyResult.conflicts.length > 0`) and conflict-resolution errors.

**M10+ post-release polish** (post-M10, same milestone bucket):

- **Already-applied commit detection (`git cherry`)**: new RPC `git.cherry` (`sidecar/internal/git/cherry.go`) wraps `git cherry <target> <source> [<source>~N]`. Returns SHAs marked `-` (patch-equivalent already present on target). `MaxCount` arg (`<source>~N`) limits comparison window to the commit list size to keep it fast on large repos. Fallback: if `<source>~N` doesn't exist (branch shorter than `MaxCount`), retry without limit. Errors fall through to `[]` (best-effort — caller treats as "unknown").
- **Dim + disable applied checkboxes**: `appliedShas: Set<string>` state in `+page.svelte`, passed as `applied` prop to `CommitList`. `{@const isApplied = applied.has(c.sha)}` on each row → `class:applied` (opacity 0.55) + `disabled` on checkbox + Space key skip + `pointer-events: none` on `.cb-wrap`. Title attribute differs: "Already applied to target branch" vs queue add/remove.
- **"Applied" red badge**: small red-bordered uppercase `Applied` label appears to the right of each dimmed commit row (flex item after `.commit-info`). CSS: `color: #e05555; border: 1px solid #e05555; border-radius: 3px; padding: 1px 5px;`. Reinforces the disabled state visually beyond opacity.
- **`refreshApplied()`**: pulls applied set via `rpc.git.cherry(repo, sourceBranch, targetBranch, settings.maxCommits)`. Called atomically with `loadCommits` (see double-load fix below). Also called after successful cherry-pick, after `continueCherry`, and when target branch changes via the dropdown.
- **Empty-commit auto-skip in batch cherry-pick (replaces hard error)**: `cherrypick.go` previously aborted the entire batch when one commit was already applied (empty diff). Now: detects empty result (`ConflictFiles` returns 0 files after `git cherry-pick <sha>` exit-1), runs `git cherry-pick --abort` to clear `CHERRY_PICK_HEAD`, appends SHA to `result.Skipped`, then `continue` to the next SHA. Matches TortoiseGit's "Skip" prompt behavior.
- **`CherryPickResult.Skipped []string`**: new field on the Go struct and TypeScript type (`rpc-types.ts`). Frontend receives skipped list alongside `applied` and `conflicts`.
- **Skipped commits → toast + auto-dim**: after `cherryPick` resolves, frontend awaits `refreshApplied()` first, then unions `pickResult.skipped` into `appliedShas` so the skip survives `git cherry`'s patch-ID limitation (see below). Two separate toasts: success ("Applied N commits → main") and warning ("Skipped M commits — already applied" with commit subjects + short SHAs as detail). If `nApplied === 0 && nSkipped > 0`, only the warning toast is shown.
- **`ContinueCherry` empty-after-resolve handling**: after a conflict is resolved manually, `git cherry-pick --continue --no-edit` may itself fail with "The previous cherry-pick is now empty, possibly due to conflict resolution" — e.g. when the user accepted only "theirs" and that content was already on `main`. `conflict.go` now inspects the error message for `"now empty"` / `"cherry-pick --skip"` and retries with `git cherry-pick --skip`, succeeding silently instead of leaving the repo stuck.
- **Why `git cherry` can't always detect already-applied**: `git cherry` compares patch IDs (hash of the diff). When a commit is cherry-picked with conflicts and the user resolves them differently, the resulting patch on target is NOT identical to the original — patch IDs diverge, `git cherry` marks it `+` (unapplied), but `git cherry-pick` still produces an empty commit because the *final state* already matches. This is a fundamental limitation; the runtime auto-skip + dim covers it.
- **Remote branches in dropdown**: `git.branches` always called with `includeRemote: true` from the frontend so BranchSelect search finds both local and remote branches after a fetch. `Branch.remote: boolean` field added to TS type. Remote branches render with italic gray name + small uppercase "remote" badge (border `#6da4dd`). Pseudo-ref `refs/remotes/origin/HEAD` filtered out by sidecar.

**Double-load fix (bright→dim flash on repo open / branch switch)**: The commit list briefly rendered with all commits at full opacity, then dimmed ~1.5s later. Root causes were three separate issues stacked together — all relate to how state updates flush in Svelte 5:

1. **Slow `git cherry` on full branch history**: original RPC didn't limit history depth. Fix: added `MaxCount` arg in `cherry.go` so it only compares the last `settings.maxCommits` commits (same window the UI shows). With fallback for short branches.
2. **`loadCommits` and `refreshApplied` ran sequentially via separate awaits**: each `await` is a microtask boundary, so Svelte flushes pending state changes between them. `commits = <fetched>` triggered render-1 (commits visible at full opacity), then `appliedShas = new Set(...)` triggered render-2 (commits dimmed). Fix: both RPCs run via `Promise.all` inside `changeSourceBranch` and `openRepo`, and BOTH state assignments happen in the same synchronous block after the await — Svelte 5 batches synchronous reactive writes within the same microtask into one render.
3. **`autoFetchOnOpen` triggered a visible second load**: after the initial atomic render completed, the post-open `git fetch` block called the old `loadCommits(sourceBranch)` which set `loadingCommits = true` + `commits = []` again → user saw a fresh "Loading commits…" message and the list disappear/reappear seconds after the initial load. Fix: the post-open refresh now does fetch + branches + commits + cherry via `Promise.all` and assigns all four state vars atomically WITHOUT touching `loadingCommits`. If the remote has new commits, the user sees one quiet swap; if not, no visible change at all.
4. **Initial `commits = []` clear on branch switch**: even with the Promise.all batch, the synchronous setup block in `changeSourceBranch` was setting `commits = []` and `appliedShas = new Set()` before the await — this rendered an empty list / spinner first, then the new data. Fix: removed those clears; old list stays visible (with `class:is-loading` adding `opacity: 0.5; pointer-events: none` and a 0.12s transition) until the new data arrives, then swaps in atomically. CommitList template also guards: `{#if loading && commits.length === 0}` so the "Loading commits…" message only appears on truly empty state (initial open).
5. **`openRepo` had three sequential awaits with state changes between**: `await openRepo` → set `repoPath`, `currentBranch` → `await branches` → set `branches`, `sourceBranch`, `targetBranch` → set `loadingCommits = true`, `commits = []` → `await Promise.all`. Each await is a microtask boundary; Svelte flushed renders between each, so the user saw the UI populate piecewise (toolbar → branches dropdown → loading message → commits). Fix: all fetched values go into local consts (`r`, `branchList`, `newSource`, `newTarget`, `newCommits`, `appliedList`) — NO `$state` assignments happen until after the final await. Then 8 state assignments in a row (`repoPath`, `currentBranch`, `branches`, `sourceBranch`, `targetBranch`, `commits`, `appliedShas`, `loadingCommits = false`) all batch into a single render.

  Net effect: workspace appears in one atomic transition. Because the workspace itself is gated behind `{#if repoPath}`, the user sees: empty welcome screen → fully-loaded workspace with correct applied-state dimming in one render. No bright→dim flash, no piecewise UI population.

**M14a done** (GitHub + GitLab PR creation MVP):
- **Forge module** `app/src-tauri/src/forge.rs`: `Provider` trait (`async_trait`) with GitHub + GitLab impls. Uses `reqwest` (rustls-tls) for HTTP, `keyring` crate for OS-native token storage (Windows Credential Manager / macOS Keychain / Linux Secret Service). Token key format `<kind>:<host>:<username>` under service `lazy-cherry-pick.forge`.
- **Architecture decision**: Rust handles all forge HTTP requests directly (NOT via sidecar). Token never crosses IPC boundary back to frontend after save. Sidecar continues to own git CLI ops only.
- **Tauri commands**: `forge_test_connection`, `forge_save_connection`, `forge_delete_connection`, `forge_create_pr`. Token flows Frontend → Rust (input) → keychain → Rust (load on use). On `forge_save_connection`, token saved to keychain first; if it fails, settings.json is not polluted. `forge_delete_connection` scans remaining connections — if another repo still references the same `(kind, host, username)` triple, the keychain entry is KEPT (multiple repos can share one PAT); only when the triple is no longer referenced does the keychain entry get deleted.
- **`AppSettings.forge_connections: HashMap<repo_path, ForgeConnection>`**: per-repo connection metadata (kind + baseURL + host + username). Token is NOT in settings.json — only metadata to locate the keychain entry.
- **GitHub provider** (REST v3, `Authorization: Bearer`): `/user` for test (returns `login` + `x-oauth-scopes` header); `/repos/{owner}/{repo}/pulls` POST for create PR with `{title, body, head, base, draft}`. PAT classic + fine-grained both supported.
- **GitLab provider** (REST v4, `Authorization: Bearer`): `/api/v4/user` for test; `/api/v4/personal_access_tokens/self` for scope discovery; `/api/v4/projects/{urlencoded_path}/merge_requests` POST with `{source_branch, target_branch, title, description}`. URL-encode project path (handles subgroups). Draft prepended as `"Draft: "` prefix in title (GitLab convention).
- **`ConnectForge.svelte`**: modal — provider segmented control, baseURL + host + token inputs, "Test connection" button → shows username + scopes if successful, "Save & connect" stores to keychain via `rpc.forge.saveConnection`. Auto-fills baseURL/host from current repo's remote URL via `parseForge` from M13. GitHub Enterprise / GitLab self-managed users edit baseURL manually.
- **`CreatePR.svelte`**: modal — base/head branches (base is dropdown of local-only branches, head is read-only the pushed branch), title (auto-fill from last commit subject), description (bullet list of commit subjects, or single commit subject if 1), draft checkbox. Submits via `rpc.forge.createPR`, dialog closes on success, parent shows toast with PR URL + auto-opens browser.
- **PickQueue dropdown extension**: when `forgeConnected = true`, dropdown adds divider + "Apply & Push & Create PR to <remote>" entries (one per remote). New callback `onapplypushpr(remote)`. Mode type extended: `"apply" | "apply-push" | "apply-push-pr"`.
- **Settings → Connected accounts section**: shows current repo's connection status (Connect / Reconnect / Disconnect). Disconnect deletes keychain entry + removes settings.json metadata (idempotent — silently succeeds if keychain entry missing). Connect button opens `ConnectForge` modal.
- **End-to-end flow**: queue → click "Apply & Push & Create PR to origin" → cherry-pick → push → CreatePR dialog auto-opens with pre-filled fields → user reviews → submit → Rust loads token from keychain → POST to forge API → success toast with PR URL + browser opens.

**Dependencies added**: `keyring = "3"`, `reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }`, `async-trait = "0.1"`. Cold rebuild bloat ~150 MB target/ directory.

**Limitations / future work**:
- Bitbucket NOT implemented (deferred per scope decision).
- OAuth not supported — PAT only. Users get tokens manually from GitHub/GitLab settings UI.
- `ForgeKind` in `rpc-types.ts` is `"github" | "gitlab"` (strict, mirror Rust enum), `DetectedForgeKind` in `forge.ts` is `"github" | "gitlab" | "bitbucket" | "unknown"` (loose, from URL detection). Renamed to disambiguate — Bitbucket detected URLs fall back to GitHub default in ConnectForge for now.
- ~~No "list PRs of current branch" panel~~ → done in M14b (see below).
- Auto-detect prompt on `openRepo` ("Connect this repo to GitHub?") not implemented — user discovers Connect via Settings.

**M14a polish** (during M14 implementation, before M14b):
- **Smart default PR base** (`git.defaultBranch` RPC): new `sidecar/internal/git/defaultbranch.go` resolves via `git symbolic-ref --short refs/remotes/<remote>/HEAD` → strips remote prefix → returns the upstream default branch (e.g. `"main"`). Falls back to `main` → `master` → `develop` if symbolic-ref unset. Loaded atomically in `openRepo` alongside `branches`/`remotes`. `pickDefaultBase()` in `+page.svelte` uses it so CreatePR dialog's base dropdown defaults sensibly even on repos that use `develop` or `trunk`. Skips returning the head branch (PR head == base is invalid).
- **Detached HEAD cherry-pick fix** (`sidecar/internal/git/cherrypick.go`): previously the sidecar errored out immediately when HEAD was detached. But if the caller provided a `target` branch, the `git checkout target` call later would fix the detached state automatically. Fix: only return the detached error when BOTH `args.Target` is empty AND HEAD is detached (nowhere to switch to). User-facing message also changed from "switch to a branch first" to "pick a target branch in the dropdown" to point at the right control.
- **Frontend target reset for invalid branches** (`PickQueue.svelte`): `$effect` watches `localBranches` (filtered from `branches.filter(b => !b.remote)`) and auto-switches `targetBranch` if it's holding a stale remote name (e.g. `origin/main` from before the local-only filter was added) or empty (repo opened in detached HEAD). Picks the `isHead` local branch first, then first local branch.
- **Roundtrip verify on keychain save** (`lib.rs` `forge_save_connection`): after `token_save`, immediately calls `token_load` with the same key to verify the entry is actually retrievable. Catches platform-specific keychain bugs where set succeeds but get fails. Returns descriptive error including the exact key used (`'<kind>:<host>:<username>'`) for diagnostics. Settings.json is only updated after this verification passes.
- **Explicit keyring features** (`Cargo.toml`): `keyring = { version = "3", features = ["windows-native", "apple-native", "linux-native-sync-persistent"] }` — relying on default features risked a no-backend build on some toolchain configurations where defaults differ.
- **Shared-token retention on Disconnect** (`forge_delete_connection`): now scans remaining `forge_connections` for any other repo referencing the same `(kind, host, username)` triple before deleting the keychain entry. If another repo still references it, the keychain entry is kept (user has 5 personal repos all using one PAT → disconnecting one doesn't break the others). Only when the triple is no longer referenced does the keychain entry get deleted.
- **Error message URL exposure** (`forge.rs` `extract_error`): now includes the request URL and parses GitHub's `errors[]` array + GitLab's `error_description` so 422 "Validation Failed" surfaces the real cause (e.g. "No commits between main and feature", "A pull request already exists for ..."). Helps diagnose 404s (wrong project path) and 422s (validation failures) without server-side logging.
- **`pendingFinalize` state survives conflict resolution** (`+page.svelte`): when user picks "Apply & Push & Create PR" but the cherry-pick stops mid-way for a conflict, the push/PR intent was previously lost — user resolved the conflict, got the cherry-pick toast, and had to manually file the PR. Fix: `pendingFinalize: { remote, createPr } | null` is set by `applyPick`/`applyPushCreatePR` before kicking off cherry-pick, and consumed by both `applyPickShas` success and `continueCherry` success (including the recursive path that re-applies remaining shas). `finalizeAfterApply()` helper does the push + opens the dialog. Cleared on abort/cancel.

**M14c done** (Bitbucket Cloud support + UI polish):
- **`BitbucketProvider`** (`app/src-tauri/src/forge.rs`): REST 2.0 Cloud. Auth via Basic Auth — reqwest `.basic_auth(user, Some(token))` handles base64. `ForgeKind::Bitbucket` variant added; `provider_for(kind, base_url, username)` signature extended (username required for Bitbucket, ignored by GitHub/GitLab). project_path format `workspace/repo_slug` same as GitHub.
- **Atlassian API Token migration**: Atlassian announced App Password removal — controlled brownouts from 2026-06-09, full removal 2026-07-28. ConnectForge now defaults Bitbucket to API Token flow:
  - Username field shows "Atlassian account email" + `type=email` + placeholder `you@example.com`
  - Token field shows "API Token" + placeholder `ATATT3xFfGF0…`
  - Help link → `https://id.atlassian.com/manage-profile/security/api-tokens`
  - Required scopes documented: `read:user:bitbucket, read:repository:bitbucket, read:pullrequest:bitbucket, write:pullrequest:bitbucket`
  - ⚠ Yellow warning banner in form: "Atlassian is removing App Passwords on 2026-07-28. Use an API Token with scopes."
  - Auth model logic unchanged — both App Passwords (legacy) and API Tokens (new) work with same Basic Auth flow until brownout.
- **Bitbucket-specific provider behavior**:
  - **test_connection**: `GET /2.0/user` → returns `username` or `nickname`. Empty scopes (Bitbucket /user doesn't expose them).
  - **create_pr**: `POST /2.0/repositories/{ws}/{repo}/pullrequests` with `{title, description, source: {branch: {name: head}}, destination: {branch: {name: base}}, close_source_branch: false}`. Draft convention via `"Draft: "` title prefix (Bitbucket has no native draft flag).
  - **list_prs_for_branch**: BBQL query `state="OPEN" AND source.branch.name="<head>"` URL-encoded. Returns from `body.values[]` array. Author from `author.nickname` or `author.display_name`. Draft detected via title prefix.
  - **find_existing_pr**: reuses `list_prs_for_branch`, filters by base. Triggered on HTTP 400 with "already exists" (Bitbucket's status code differs from GitHub 422 and GitLab 409).
- **Frontend integration**:
  - `ForgeKind` TS type union expanded to `"github" | "gitlab" | "bitbucket"`
  - `rpc.forge.testConnection(kind, baseUrl, token, username?)` — `username` param optional, only required for Bitbucket
  - `ConnectForge.svelte` provider segmented control: GitHub / GitLab / **Bitbucket**. Username field conditional via `$derived needsUsernameUpfront = kind === "bitbucket"`
  - `Settings.svelte` connection label: "GitHub" / "GitLab" / **"Bitbucket"**
  - `CreatePR.svelte` dialog title: "GitHub PR" / "GitLab MR" / **"Bitbucket PR"**
  - Toast label logic in `+page.svelte`: `kind === "gitlab" ? "MR" : "PR"` (so Bitbucket also says "PR")
- **CreatePR dialog polish**:
  - **Required-field indicator**: Title label has red asterisk `<span class="required">*</span>` + HTML `required` attribute for browser-level validation. JS-side validation still in `submit()` (`if (!title.trim()) error = "Title is required"`).
  - **Auto-fill title on "+" direct flow**: previously `lastAppliedCommits = []` → empty title. Now `openCreatePrDirect()` fetches the latest commit on the target branch via `rpc.git.commits(repoPath, targetBranch, 1, 0, {})` and passes it as `appliedCommits` → `defaultTitle()` returns that subject. User can still edit. Best-effort: empty list on RPC error.

**M14b done** (PR discovery + rich UI):
- **`forge.list_prs_for_branch` Provider trait method**: returns `Vec<PrSummary>` — open PRs/MRs whose head is `<branch>`. GitHub: `GET /repos/{owner}/{repo}/pulls?state=open&head={owner}:{head}`. GitLab: `GET /api/v4/projects/{urlencoded}/merge_requests?state=opened&source_branch={head}`. Both implementations parse `title`, `base/target_branch`, `author` (`user.login` / `author.username`), `updated_at`, `created_at`, `body/description` (truncated to 200 chars via `truncate_text()` helper).
- **`PrSummary` enriched type**: `url`, `number`, `title`, `state`, `draft`, `base`, `author`, `updated_at`, `created_at`, `body_preview` (mirrored in TS `PRSummary` interface).
- **`forge_list_prs` Tauri command**: loads token from keychain, calls `provider.list_prs_for_branch`. Same auth flow as `forge_create_pr` — token never crosses IPC boundary.
- **`find_existing_pr` / `find_existing_mr` providers**: when `POST /pulls` returns 422 "already exists" (GitHub) or 409 / 422 (GitLab), the provider auto-fetches the matching open PR/MR via GET. Returns `CreatePrResult { already_existed: true }` instead of bubbling up an error. Frontend swaps toast wording: success "PR #N created" vs warning "PR #N already exists for this branch" — both with the clickable URL in detail.
- **`CreatePrResult.already_existed` field**: serializes to camelCase `alreadyExisted` for TS. Default false on the success path; only true when `find_existing_*` resolves it.
- **PR status bar in PickQueue** (between target-branch row and queue body, only when `forgeConnected`):
  - **Empty state**: italic muted "No open PR for `<branch>`"
  - **Loading state**: "Checking PRs…" (while `prCheckLoading` is true)
  - **PR present**: green dot + "Open PR:" (or "N open PRs:" for multiple) + clickable `#42`/`#42 (draft)` badges
  - Background color shifts to subtle accent tint (`rgba(74, 126, 245, 0.08)`) when there IS a PR — quick visual confirmation
  - Refresh `↻` button (calls `onrefreshprs`)
  - **Create-direct `+` button** (next to refresh): opens CreatePR dialog WITHOUT requiring any cherry-pick — for the case where the user already has commits on the branch (pushed via terminal or another tool) and just wants to file a PR. Auto-picks the first remote that parses to a forge project path.
- **Hover popover on each PR badge** (`PickQueue.svelte`, inline component):
  - Triggered on `mouseenter`, hides on `mouseleave` with 150ms grace period so the user can move the cursor INTO the popover (to click "Open in browser" or read body).
  - Shows: title (with DRAFT chip if draft), `→ base`, `by author`, `updated <relTime>`, `opened <relTime>` (only if different from updated), body preview (max 100px scrollable), and "Open #N in browser ↗" button.
  - `relTime()` helper: "just now" / "Nm ago" / "Nh ago" / "yesterday" / "Nd ago" / "Nw ago" / locale date. Hover on the displayed time reveals the full ISO timestamp via native title attribute.
- **Auto-refresh PR list via `$effect`**: watches `forgeConnection`, `repoPath`, `targetBranch`, `remotes`. Triggers `refreshTargetPRs()` on any change. Also called after a successful `handleCreatePR` so the bar reflects the new PR immediately (no need to manually refresh).
- **Best-effort fetch**: API failures (token expired, network, permission) silently leave the list empty and don't show error toasts — the status bar is a background helper, not a hard requirement for the user's main workflow.
- **Toast URL detail is now clickable** (`Toast.svelte`): if `detail` is a single URL string, it renders as a button (`button.detail-link`) that calls `onurlclick(url)` (wired to `openExternalUrl` in `+page.svelte` → `tauri-plugin-opener`). PR-created toasts no longer auto-open the browser — user clicks the URL in the toast when they want.
- **Multiple PRs from same head**: status bar lists all open PRs (a head branch CAN have multiple open PRs with different bases — `feature/x → main` + `feature/x → develop` coexist). Each badge is a separate hover popover, click opens the corresponding PR.

**M13 done**:
- **`git.remotes` RPC**: new `sidecar/internal/git/remotes.go` runs `git remote -v`, merges fetch/push pair per remote, returns `[{name, fetchUrl, pushUrl}]`. Order preserves git output. Registered in `main.go`.
- **Per-remote push dropdown in PickQueue**: flat list shows "Apply" + "Apply & Push to <remote>" for each configured remote. State `pushRemote` defaults to `"origin"` and auto-falls-back if removed. Button label includes remote: `Apply & Push N commits → origin/main`. Frontend signature `onapplypush(remote)` → `applyPick(true, remote)` → `applyPickShas(shas, true, remote)` → `rpc.git.push(repo, branch, remote)` (the `remote` param was already plumbed end-to-end; only the UI was missing).
- **Forge URL builder** (`app/src/lib/forge.ts`): pure utility — `parseForge(remoteUrl)` extracts `{kind, host, path, webBaseUrl}` from HTTPS / SSH / `git@` URLs. `forgeCommitUrl`, `forgeBranchUrl`, `forgeFileUrl` build provider-specific paths (GitHub: `/commit/`, GitLab: `/-/commit/`, Bitbucket: `/commits/`). Detection via host suffix. No network calls.
- **CommitDetail copy + open-in-browser buttons**: small icon buttons (clipboard + external-link SVGs) inline next to commit subject and SHA. `oncopy(label)` callback fires Toast "X copied"; `onopenurl(url)` uses `@tauri-apps/plugin-opener` `openUrl()` to open in system browser. Open button only renders when forge is detected from a remote URL (preference order: `origin` first, then first remote with recognized forge, then any). Self-hosted GH Enterprise / GitLab self-managed via host pattern `gitlab.*` / `bitbucket.*` still get detected; unknown hosts skip the open button.
- **Atomic remote load**: `openRepo` fetches `branches` and `remotes` via `Promise.all` and assigns both in the same sync block — no extra render. `doFetch`/`doPull` do NOT refresh remotes (rare to change via fetch); user must reopen repo if they `git remote add` externally.

**M15a done** (MCP server — sidecar as AI tool provider):
- **Dual-mode binary**: `sidecar/main.go` checks `os.Args[1] == "--mcp"` as the very first thing in `main()`. If set, runs `mcp.NewServer(os.Getenv("LCP_DEFAULT_REPO")).Serve(...)` and returns — the NDJSON RPC path (`internal/rpc`, used by the Tauri app) is never reached. Tauri's `sidecar_call` never passes `--mcp`, so this is **zero-impact** on the desktop app.
- **`sidecar/internal/mcp/` (new package)**: hand-rolled MCP (Model Context Protocol) server, JSON-RPC 2.0 over stdio, stdlib-only (no SDK dependency, matching the project's no-library philosophy).
  - `server.go` — transport + dispatch: `initialize`, `notifications/initialized`/`notifications/cancelled` (no response), `ping`, `tools/list`, `tools/call`. BOM-stripping and line scanning mirror `internal/rpc/server.go`. `tools/call` wraps results in `{content[], structuredContent, isError}` per MCP spec; unknown tool name → `isError: true` (tool error, not protocol error) so the model can recover.
  - `tools.go` — registers **22 tools** (13 read-only, 9 write) mapping 1:1 to `internal/git` methods. Schema helpers `obj/str/num/boolp/arrStr`. `resolveRepo()`: explicit `repo` arg → `LCP_DEFAULT_REPO` env → error.
  - `server_test.go` — 7 tests (initialize, notification-no-response, tools/list shape, unknown tool/method, repo resolution, conflict-marker guard). All pass.
- **`sidecar/internal/git/compare.go` (new)**: `CompareBranches()` = `git log base..head`, for the `compare_branches` tool ("what would a PR from head into base contain"). `commits.go` now exposes `parseCommits(out string) []Commit` as a shared helper (extracted from `Commits()`), reused here.
- **AI-driven conflict-resolution loop** (per explicit user request — "AI review lại conflict resolve và tự động/suggest fix conflict"): `cherry_pick` does **not** hard-error on conflict. It catches `*git.Error{Code: CodeCherryPickConflict}` and returns `structuredContent: {status: "needs_human_resolution", applied, conflicts, hint}` with `isError: false`. The hint walks the AI through `get_conflict_content` → propose merge → `resolve_conflict_file` → `continue_cherry_pick` (or `abort_cherry_pick`). `resolve_conflict_file` refuses content containing `<<<<<<<`/`=======`/`>>>>>>>` (`containsLineMarker`, checks line-start only). Tool descriptions explicitly say to get user approval before resolving — MCP doesn't enforce this, it's client/UX responsibility.
- **13 tools (initial)**: read — `list_commits`, `list_branches`, `get_commit_detail`, `find_already_applied`, `dry_run_pick`, `compare_branches`, `default_branch`, `get_conflict_files`, `get_conflict_content`; write — `cherry_pick`, `abort_cherry_pick`, `resolve_conflict_file`, `continue_cherry_pick`. Full schemas + per-tool docs in `docs/MCP.md`.
- **Architecture note**: forge (PR/MR creation) deliberately NOT exposed via MCP — that logic lives in Rust (`forge.rs`) with OS-keychain token storage, not the Go sidecar. Porting it (Go HTTP client + `go-keyring` reading the same `lazy-cherry-pick.forge` keychain entries) is **M15b**. MCP Sampling (server asks the *client's* model to generate text) is **M15d**, not implemented.
- **Docs**: `docs/MCP.md` — protocol, full tool catalog, conflict loop walkthrough, Claude Desktop / Cursor config example (`{"mcpServers": {"lazy-cherry-pick": {"command": "...sidecar-x86_64-pc-windows-msvc.exe", "args": ["--mcp"], "env": {"LCP_DEFAULT_REPO": "..."}}}}`).
- **CI publishes standalone sidecar binary**: `.github/workflows/release.yml` now adds a step after `tauri-action` that runs `gh release upload <tag> app/src-tauri/binaries/sidecar-$triple.exe --clobber` — attaches the sidecar `.exe` as its own release asset (separate from the `.msi`), so MCP users can download just that file without installing the desktop app. Release body has a new "MCP server" section linking to `docs/MCP.md`.
- **`initialize` returns `instructions`**: `serverInstructions` const in `server.go` — a model-facing hint (per MCP spec) summarizing the suggested workflow and the conflict-resolution loop, so clients that surface `instructions` give the model this context automatically without `docs/MCP.md` being pasted into the conversation. `TestInitialize` asserts it's non-empty.
- **App version bumped to 0.15.0** (package.json, tauri.conf.json, Cargo.toml/Cargo.lock, `serverVersion` in `server.go`) — local installers built (`Lazy Cherry Pick_0.15.0_x64_en-US.msi` / `_x64-setup.exe`) for testing MCP as an end user.

**M15a extended** (9 more MCP tools, per user request — "thêm push cho tôi, thêm tất cả các tính năng của tool desktop còn tạo PR thì để sang M15b"): added in `tools.go` without bumping the milestone number, since these are the same kind of thin `internal/git` wrappers as the original 13.
- **4 new read tools**: `get_status` (wraps `Status()` — branch, detached, upstream, ahead/behind, staged/unstaged/untracked, useful before any write op), `list_remotes` (wraps `Remotes()`), `get_file_diff` (wraps `FileDiff()` — unified diff of a file in a commit), `get_staged_diff` (wraps `StagedFileDiff()` — review a staged conflict resolution before `continue_cherry_pick`).
- **5 new write tools**: `fetch` (wraps `Fetch()`), `pull` (wraps `Pull()`, fast-forward only), `push` (wraps `Push()` — the explicitly-requested tool; description emphasizes it touches SHARED remote state and needs user confirmation), `create_branch` (wraps `CreateBranch()`), `resolve_conflict_side` (wraps `ResolveConflict()` — one-step ours/theirs alternative to `resolve_conflict_file`, mirrors the desktop ConflictResolver's "Keep Ours / Use Theirs" buttons).
- **22 tools total** (13 read-only, 9 write). `serverInstructions` rewritten to cover the new tools (status/remotes checks before sync ops, push confirmation, resolve_conflict_side/get_staged_diff in the conflict loop). `server_test.go` must-have list extended; `tools/list` count assertion raised `>=10` → `>=20`.
- **Still excluded from MCP** (unchanged from original M15a scope): forge/PR tools (→ M15b) and the M8 external-diff/merge-tool extraction RPCs (`git.extractDiffFiles`/`extractConflictFiles`/`stageResolvedFile`/`cleanupTmpDir`) — desktop-GUI-only, no AI use case. `git.commitFiles` was not added as a standalone tool — its data is already returned inside `get_commit_detail`'s `files` field.
- Rebuilt sidecar binary; `go test ./internal/mcp/ -v` and `./internal/git/ -v` pass; smoke-tested `tools/list` (22 tools) and `get_status`/`list_remotes` against this repo via `--mcp`.

**M15a npm distribution** (same day, per user request — "có thể đưa lên npx/npm để tạo gói cài đặt mà ko cần phải tải file exe trên github để sử dụng qua AI ko"): added `mcp-npm/` — an npm wrapper package `lazy-cherry-pick-mcp` so AI clients can run `npx -y lazy-cherry-pick-mcp` instead of downloading the sidecar `.exe` from GitHub Releases manually.
- **Chosen approach**: download-on-run wrapper (Option A), Windows x64 only for v1. `mcp-npm/bin/cli.js` is zero-dependency stdlib Node: detects `process.platform`/`process.arch`, downloads `sidecar-x86_64-pc-windows-msvc.exe` from `https://github.com/tentaikhoan24/lazy-cherry-pick/releases/download/v<pkg.version>/...` (follows redirects, GitHub release assets 302 to S3), caches it under `%LOCALAPPDATA%\lazy-cherry-pick-mcp\v<version>\`, then `spawnSync(bin, ['--mcp'], {stdio: 'inherit'})`. Other platforms get a clear error pointing to build-from-source.
- **`package.json`**: `os: ["win32"]`, `cpu: ["x64"]` so npm/npx refuses install on unsupported platforms with a clean `EBADPLATFORM` before even running `cli.js`. `bin: {"lazy-cherry-pick-mcp": "bin/cli.js"}`.
- **Version is the release pointer**: `mcp-npm/package.json` version MUST equal the app version (i.e. the `vX.Y.Z` GitHub release tag) — it's used directly to build the download URL and the cache path. **Every "chore: bump version to X.Y.Z" commit must also bump `mcp-npm/package.json`.**
- **CI** (`.github/workflows/release.yml`): added `registry-url: 'https://registry.npmjs.org'` to the setup-node step; new step "verify mcp-npm version matches tag" fails the release if `mcp-npm/package.json`'s version ≠ the pushed tag; new step "inject sidecar checksum into mcp-npm/package.json" computes SHA256 of the built `sidecar-<triple>.exe` and writes it into `mcp-npm/package.json`'s `sidecarSha256` field (CI workspace only, not committed) right before publish; new step "publish npm MCP wrapper" runs `npm publish --access public` from `mcp-npm/` using `NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}`.
- **Caveat**: if `releaseDraft: true` ever results in an actual draft release, the `npx` download will 404 until the release is published (GitHub doesn't serve draft-release assets). Documented in `mcp-npm/README.md` troubleshooting.
- **Docs updated**: `docs/MCP.md` (new "Running via npx" section + npx config in "Getting the binary"/"Client configuration"), `README.md` + `README_VN.md` (MCP section now leads with the npx config, binary-path config kept as alternative), `mcp-npm/README.md` (npm package page).
- **Published**: `lazy-cherry-pick-mcp@0.15.0` is live on npm (manually published 2026-06-13 via a bypass-2FA granular access token, ahead of the CI publish step landing). Verified end-to-end via `npx -y lazy-cherry-pick-mcp@0.15.0` (`initialize` + `tools/list` → 22 tools, binary cached under `%LOCALAPPDATA%\lazy-cherry-pick-mcp\v0.15.0\`).
- **Integrity fix (Socket.dev flag, 2026-06-13)**: Socket flagged `bin/cli.js` medium-risk — "downloads a native executable ... and executes it without any visible integrity/authenticity verification". Fix: `mcp-npm/package.json` gains a `sidecarSha256` field (empty string in git); the new CI step above sets it to the SHA256 of the exact binary uploaded to the release before `npm publish`. `cli.js` streams the downloaded file through `crypto.createHash('sha256')` and refuses to cache/exec on mismatch (deletes the tmp file, exits 1 with expected/actual hashes). If `sidecarSha256` is empty (versions published before this change, e.g. v0.15.0), `cli.js` logs a stderr warning and proceeds. Takes effect starting the next tagged release (v0.16.0+).

**M16 done** (AI conflict resolution via Claude Code CLI — "desktop app gọi ngược Claude Code để suggest fix conflict"):
- **Direction**: the desktop app shells out to the user's installed `claude` CLI in headless mode (`-p`) to resolve cherry-pick conflicts. NOT via MCP (MCP is the opposite direction — Claude Code calling *into* the sidecar). Uses the user's existing Claude Code login (Pro/Max or API key) — no separate Anthropic API subscription, no token stored/passed by the app.
- **Stability principle**: never parse Claude's stdout to extract the merged content. Claude Code is an agent with Edit/Write — it writes the merged file straight to disk; the app re-reads the file from disk. The JSON stdout (`--output-format json`) is used only for success/cost/error.
- **Go (2 new RPC)**: `git.diffTexts(leftText, rightText)` → `FileDiffResult` (`sidecar/internal/git/difftexts.go`; `git diff --no-index --unified=99999` on temp files; treats exit 1 as "differences found"). `git.restoreConflict(repo, file)` → `git checkout -m -- <file>` (rebuilds conflict markers from index stages :1/:2/:3, for Discard). Both registered in `main.go`; `git.diffTexts` uses a zero-value `git.Repo` (no repo needed).
- **Rust (`lib.rs`)**: `detect_claude()` → finds `claude.cmd` (`%APPDATA%\npm\claude.cmd` first, then `where claude`, filtering to `.cmd`/`.exe`), returns `{found, path, version}`. `run_claude_resolve(repoPath, claudePath, model, prompt, timeoutSecs, maxBudgetUsd?)` → spawns `cmd /C <claude.cmd> -p --output-format json --allowedTools "Read,Edit,Write,Glob,Grep" --disallowedTools "Bash" --permission-mode acceptEdits [--model X] [--max-budget-usd N]` with `current_dir(repo)`, **prompt fed via STDIN** (not argv — avoids cmd.exe quoting bugs, same class as the M8 args-template pitfall), `tokio::time::timeout` + `kill_on_drop`. Parses `result`/`is_error`/`total_cost_usd`/`duration_ms`. Do **not** use `--bare` (it forces ANTHROPIC_API_KEY and skips the OAuth/subscription login). `AppSettings` +4 fields: `claudeEnabled`/`claudePath`/`claudeModel`/`claudeTimeoutSecs` (all `#[serde(default)]`).
- **Safety**: `--disallowedTools Bash` blocks all git/shell/network, so the index stays unmerged (→ `git checkout -m` discard works) and Claude can't commit/push. `--permission-mode acceptEdits` auto-applies edits without hanging; workspace-trust dialog auto-skips under `-p`. Timeout + optional `--max-budget-usd`. After resolve, each file is re-read and verified to contain no `<<<<<<<`/`>>>>>>>`; `writeAndStageFile` keeps its M8 marker guard. **Review is mandatory before staging.**
- **Frontend flow** (`+page.svelte`): "🤖 AI resolve all" in `ConflictResolver` → backs up each conflict file's original (markers) content into `aiBackup` → `run_claude_resolve` (one call, all files, so Claude sees the whole picture) → re-reads files into `aiResolved`, marks marker-free ones `ai-pending`, toasts count + cost; files still containing markers get a warning toast. Per file: **Review** (`git.diffTexts(backup, resolved)` → `AiReviewModal` showing `FileDiff` side-by-side "Conflict original" vs "AI resolved"), **Accept** (`writeAndStageFile` → adds to `resolvedSet`, Continue becomes available), **Discard** (`git.restoreConflict` → markers back). AI state cleared on abort + on continue success.
- **New files**: `sidecar/internal/git/difftexts.go`, `app/src/lib/AiReviewModal.svelte`. **Settings.svelte**: "AI Conflict Resolution (Claude Code)" section — toggle, path + Browse + Detect (shows version), model dropdown (Default/Opus/Sonnet/Haiku), timeout. `claudeAvailable = $derived(claudeEnabled && !!claudePath)` gates the whole feature; hidden when off.
- **Caveats**: Windows-only path detection (`.cmd` shim); requires the user to have installed + logged into Claude Code (`claude` once) — a not-logged-in run surfaces a toast hint. `cmd /C` with a `claudePath` containing spaces is an unhandled edge (rare under `%APPDATA%\npm`). Verified: Go build+test+smoke, `cargo build`, `npm run check` (0 errors). Runtime end-to-end requires the user's own `tauri dev` (Hard rule #1).

**M16b done** (generalize M16 to any headless AI CLI — "có thể sử dụng các AI khác qua CLI như Claude Code ko"): refactored the Claude-specific path into a **generic engine + provider-preset** design (hybrid: engine is provider-agnostic, presets are pure data). The stability core ("agent writes merged file to disk, app re-reads — never parse stdout for content") is unchanged and provider-independent.
- **Rust**: `detect_claude`/`run_claude_resolve` → generic `detect_ai_tool(command)` / `run_ai_resolve(repoPath, command, args[], prompt, promptVia, outputFormat, timeoutSecs)`. Engine: `.cmd`/`.bat` command → `cmd /C` (npm shims); else spawn directly (cleaner argv quoting). `promptVia="stdin"` feeds prompt via STDIN (safe, Claude); `"arg"` means the prompt is already in `args` (rendered by frontend). `outputFormat="claude-json"` parses `is_error`/`result`/`total_cost_usd`; `"none"` uses exit code only (cost shown as 0). `AppSettings` claude* (4 fields) → ai* (8 fields): `aiEnabled`, `aiProvider`, `aiCommand`, `aiArgs`, `aiModel`, `aiPromptVia`, `aiOutputFormat`, `aiTimeoutSecs` (all `#[serde(default)]`; default `aiArgs` = the Claude template).
- **Frontend**: new `app/src/lib/ai-providers.ts` — `AI_PROVIDERS` preset list (Claude verified; Gemini/Codex/Aider best-effort defaults to tune during testing; Custom) + `renderAiArgs(template, model, prompt, promptVia)` (quote-aware tokenizer; `{model}` dropped-with-its-flag when empty; `{prompt}` inserted as a single arg only in `arg` mode). `Settings.svelte`: "AI Conflict Resolution" section now has a **Provider dropdown** (applies preset into editable fields), Executable+Detect+Browse, Model (dropdown or free-text), Timeout, and an **Advanced** collapsible exposing the raw `aiArgs` template + `promptVia` + `outputFormat` for live tuning without recompiling. `+page.svelte` `aiResolveAll` renders args via `renderAiArgs` and calls `run_ai_resolve`; `aiAvailable = aiEnabled && !!aiCommand`.
- **Design rationale**: everything is effectively "Custom" under the hood — built-in presets just pre-fill the editable command/args/promptVia/outputFormat fields (same spirit as M8 external-tool auto-detect). Adding a new CLI = one entry in `AI_PROVIDERS`, no engine/Rust change. Claude keeps full polish (JSON cost, model dropdown, Bash-off sandbox) because it's a complete preset.
- **Caveat**: only the Claude preset is verified. Gemini/Codex/Aider preset flags (headless mode, auto-approve-edits, prompt delivery) are first-guess defaults — they need real-run tuning, which is why `aiArgs`/`promptVia`/`outputFormat` are user-editable in Advanced. `arg`-mode prompt through a `.cmd` shim (e.g. `codex.cmd`) still routes via `cmd /C` and can hit cmd.exe quoting limits with multi-line prompts; `.exe` tools spawn directly and quote correctly. Verified: `cargo build`, `npm run check` (0 errors). No Go change. Runtime per-AI testing is the user's next step.

**M16c done** (AI review — 3-panel view + external diff/merge tool reuse, per user request — "review có thể hiển thị kiểu 3 panel giống fix conflict thủ công ... và sử dụng external diff viewer khác"): both options implemented, frontend-only, **zero Go/Rust changes**.
- **Option A — built-in 3-way view** (`AiReviewModal.svelte`): header gets a Diff/3-way toggle. "3-way" renders three read-only, line-numbered, scroll-synced panes — Theirs (incoming) / Ours (current) / AI resolved — via new local helper `splitConflictSides(rawMarkerText)` which walks `<<<<<<</=======/>>>>>>>` markers and reconstructs the full ours-text and theirs-text (mirrors the conflict-editor parser but simplified to flat text, no provisional-choice machinery). New props `originalText`/`resolvedText` (= `aiBackup.get(file)` / `aiResolved.get(file)`) feed the split. "Diff" mode is unchanged (existing `FileDiff` 2-panel). Default view is "3-way" (matches the manual conflict editor's layout); toggle is per-session UI state.
- **Option B — external merge tool reuse** (`+page.svelte` `aiReview()`): when `externalMergeEnabled && externalMergePath`, "Review" on an `ai-pending` file now launches the configured external tool via the **exact M8 pipeline** (`extractConflictFiles` → `launch_and_wait` → `stageResolvedFile` → `cleanupTmpDir`), giving a familiar 4-way base/ours/theirs/output view. **Key insight enabling zero backend changes**: `ExtractConflictFiles`'s "output" is just a copy of the current working-tree file — and since the AI CLI runs with `--disallowedTools Bash` (never `git add`), the working-tree file already holds the AI's marker-free merge while the index still has unmerged stages :1/:2/:3 for base/ours/theirs. Closing the tool auto-stages the result (same as manual conflicts) → file moves from `ai-pending` straight to `resolvedSet`. If the user leaves markers in the tool's output, `stageResolvedFile`'s marker guard throws *before* writing/staging — working tree (AI's clean merge) and index (unmerged stages) stay untouched, file remains `ai-pending`, toast shown — so `aiDiscard`/`git checkout -m` still works correctly afterward (no separate handling needed; todo item about Discard-after-auto-stage turned out to be a non-issue).
- Verified: `npm run check` 0 errors (57 warnings, unchanged — new modal reuses the existing `.overlay`/`.modal` dialog pattern). Runtime end-to-end is the user's next step (Hard rule #1).

**M16d done** (log AI CLI calls in Git Console too, per user request — "Ghi log cả phần gọi AI nữa như git"): `run_ai_resolve` invocations are now recorded into the same `git.log` file / `git-log` event stream as `[GIT_CMD]`/`[GIT_INFO]`, **frontend+Rust only, zero Go changes**.
- **`lib.rs`**: `run_ai_resolve` is now a thin wrapper (`app: tauri::AppHandle` + original params) around `run_ai_resolve_inner` (unchanged logic, renamed). After the inner call returns (success or error), the wrapper calls `log_ai_result(app, command, args, promptVia, promptChars, success, costUsd, durationMs, error)` — this appends `"ai <unix_ts> <json>"` to `git.log` via new helper `append_log_line()`, and emits the same payload (+`ts`+`type:"ai"`) as a `git-log` Tauri event. New helpers: `unix_ts()`, `ai_command_name()` (strips dir+extension, e.g. `claude.cmd` → `claude`), `sanitize_ai_args()` (in `arg`-mode prompt delivery, replaces the raw prompt text inside `args` with `<prompt: N chars>` so the log doesn't balloon with file contents/conflict text). Error path: wrapper measures its own wall-clock duration (inner may return `Err` before computing `duration_ms`, e.g. spawn failure or timeout).
- **`git_log_read`**: new `else if kind == "ai"` branch — parses `rest` as a JSON object, inserts `ts`/`type:"ai"`, returns as-is (no flattening needed since the persisted JSON already has the right shape).
- **`GitConsole.svelte`**: `LogEntry.type` extended to `"cmd" | "info" | "ai"` with optional fields `command`, `args`, `promptVia`, `promptChars`, `success`, `costUsd`, `durationMs`, `error`. New render branch: 🤖 icon + command name (purple) + args + "prompt Nc via stdin/arg" + ✓/✗ status (✗ has `title=error` tooltip + a wrapped error-detail row below) + cost (`$0.0123`, only if >0) + duration badge (reuses `.log-ms`). Header counter changed from "N commands" → "N entries" (now covers cmd/info/ai).
- Verified: `cargo build` clean, `npm run check` 0 errors (57 warnings, unchanged). No Go change, no sidecar rebuild needed. Runtime end-to-end (seeing 🤖 rows after "AI resolve all") is the user's next step.

**M16e done** (AI review 3-way now visually identical to the manual conflict editor + shared component extraction, per user requests — "3-way ko giống 3 panel khi fix conflict bằng tay" → "làm để hiển thị y hệt"; then "tách ra 1 component để cả 2 dùng chung"): **frontend-only, zero Go/Rust changes**.
- **Bug fix first**: `AiReviewModal`'s 3-way view was a flat 3-column layout with plain line-numbered panes — nothing like the manual editor. Replaced with the manual editor's exact **2-top (Theirs | Ours) + bottom (merge result)** layout: same colors (Theirs blue `#5b8def`, Ours green `#5cb85c`), inline "Conflict N/M" header bars, hatched `filler` rows, line-number gutters, resizable H-divider, scroll-synced top panes, ▲/▼ conflict nav (↑/↓ keys).
- **Shared-component refactor** (avoids duplicating the parse/render logic across the two views): extracted two new files — **`app/src/lib/conflict-parse.ts`** (pure functions `parseConflict()`/`buildRenderLines()`/`normalizeConflictText()` + `Part`/`RenderLine`/`Rendered` types, the single source of truth) and **`app/src/lib/ConflictThreePane.svelte`** (read-only viewer owning the top/bottom panes, nav, scroll-sync, H-divider, CSS). `AiReviewModal` is now a thin shell that just renders `<ConflictThreePane conflictText={originalText} mergedText={resolvedText} mergedLabel="🤖 …" />` for the 3-way branch (Diff branch still uses `FileDiff`).
- **Manual editor (`routes/conflict/+page.svelte`) de-duplicated**: removed its local `parse`/`buildRenderLines` + the 4 shared type decls; now imports them from `$lib/conflict-parse`. Its old `parse()` returned `{parts, initial}` — split into `parseConflict()` + `normalizeConflictText()` at the 2 call sites (onMount, toggleRaw). Its interactive bottom pane (drag-select, provisional choices, action buttons) stays editor-specific and is intentionally NOT shared — `ConflictThreePane` is the read-only sibling. Result: conflict-marker parsing lives in exactly one place; a future read-only conflict feature reuses `ConflictThreePane`.
- Verified: `npm run check` 0 errors (57 warnings, unchanged), 178 files (2 new). Runtime end-to-end (3-way now matching manual editor) is the user's next step.

**M11a done** (auto-stash — first step of M11 Advanced Pick, toward v1.0 = feature-complete on Windows): stash uncommitted changes before a cherry-pick batch and restore them after, so the user no longer has to commit/stash manually first.
- **Design — frontend-orchestrated** (NOT a flag inside `CherryPick`): the conflict path spans multiple RPC calls (`cherryPick` → conflict → `continueCherry` → recursive `applyPickShas`), so a single Go `AutoStash` flag couldn't manage the pop timing. Instead the frontend stashes **before** calling `cherryPick` → working tree is clean → the existing dirty-tree check (Hard rule #6) passes unchanged → pops once the whole flow truly ends. Two thin Go RPCs only; `cherrypick.go` untouched.
- **Go** (`sidecar/internal/git/stash.go`, new): `git.stash` (`git stash push [-u] -m <msg>` → `{stashed}`, false when tree already clean) + `git.stashPop` (scans `git stash list` for the entry whose message contains `<msg>`, pops only that — never a user-created stash; `{popped}`, false when none). Registered in `main.go`. Smoke-tested all 4 cases.
- **Rust**: `AppSettings.auto_stash: bool` (`#[serde(rename="autoStash", default)]`, default false).
- **Frontend** (`+page.svelte`): state `pendingAutoStashPop` + const `AUTO_STASH_MSG = "lcp-autostash"`. `maybeAutoStash()` (called at the two batch entry points `applyPick` / `applyPushCreatePR`; returns false → abort if the stash op itself errored) and `maybePopAutoStash()` (called at every terminal point: `applyPickShas` full-success + non-conflict-error branches, `continueCherry` final-success (no-remaining) branch, `abortConflict`, `cancelPick`). The conflict-pause (`-32003`) branch deliberately does NOT pop — the stash survives until continue/abort, mirroring `pendingFinalize`. `Settings.svelte`: "Auto-stash before apply" toggle (General tab, with a `.setting-hint` sub-label).
- Verified: `go build` + smoke test, `cargo build`, `npm run check` 0 errors. Runtime end-to-end is the user's next step (Hard rule #1). Next in M11: M11b (edit message + squash), M11c (partial-file pick).

**M11b done** (edit commit message + squash — second step of M11): **Go + frontend only, zero Rust changes.**
- **Edit message (per-commit):** `CherryPickArgs.Messages map[string]string` (full SHA → replacement). After each successful pick in `cherrypick.go`'s loop, a non-empty override → `git commit --amend -m <msg>`. Conflict path: `ContinueCherry` gained `ContinueCherryArgs{ Message string }` (was `_ struct{}`) → after `--continue --no-edit`, amends with the override (skipped when the commit was `--skip`ped). `main.go` `git.continueCherry` now embeds `git.ContinueCherryArgs`; **the MCP tool `continue_cherry_pick` passes `git.ContinueCherryArgs{}`** (no message — desktop-only feature, documented choice per the npm/MCP-doc-sync rule).
- **Squash:** new `git.squashCommits(repo, base, message)` in `stash.go` = `git reset --soft <base>` + `git commit -m <message>`; returns `{squashed:false}` when nothing was added (no-op for a single commit). **`base` = target tip SHA captured by the frontend BEFORE the batch** (robust across skips/conflicts — all picked commits are linear on top of base; reuses the soft-reset-after-normal-flow trick so the entire existing conflict flow is untouched). `git diff --cached --quiet` (exit 0 = nothing staged) guards the no-op.
- **Frontend** (`+page.svelte`): state `squashMode` / `squashMessage` / `messageOverrides: Map<sha,string>` / `squashBase`. `captureSquashBase()` (via `git.commits(target,1,0)` before apply, only when squashMode) runs in `applyPick` + `applyPushCreatePR`. `finalizeBatchExtras()` (squash **then** `maybePopAutoStash` — order matters; clears overrides/message/base) replaces the success-terminal `maybePopAutoStash()` calls in `applyPickShas` success + `continueCherry` final-success. `applyPickShas` passes `Object.fromEntries(messageOverrides)` to `cherryPick` (undefined when squashing); `continueCherry` passes `messageOverrides.get(conflictSha)`. Abort/cancel clear `squashBase` (no squash of a partial batch).
- **UI** (`PickQueue.svelte`): "Squash into one" checkbox in the queue header (only when ≥2 commits) → reveals a squash-message `<textarea>` (default = each subject on its own line). When NOT squashing, each queue row has a ✎ button → inline message `<input>` (override shown in amber italic + ✎ badge; ↺ resets). Apply-button label becomes "Apply & Squash …" when squashMode.
- Verified: `go build` + `go test ./internal/git ./internal/mcp`, squash + edit-message smoke-tested (reworded SHA1, squashed 2→1, no-op false), `cargo build` (no-op, no Rust change), `npm run check` 0 errors (58 warnings). Runtime end-to-end is the user's next step. Next: M11c (partial-file pick), then M12.

**M11c done** (partial-file pick — third/final step of M11, so **M11 Advanced Pick is complete**): **Go + frontend only, zero Rust changes.** Apply only a subset of a commit's files onto the target as a new commit.
- **Go** (`sidecar/internal/git/partialpick.go`, new): `git.partialPick(repo, target, sha, keep[], message?)` → dirty check → checkout target → `git cherry-pick -n <sha>` (stage all, no commit) → `git restore --source=HEAD --staged --worktree -- <unwanted>` reverts every changed file not in `keep` (handles added/deleted/modified uniformly — chose `restore` over `checkout HEAD --` precisely because `checkout` errors on added files) → `git commit -C <sha>` (reuse message+author) or `-m message`. Conflict during `-n` → `cherry-pick --quit` + `reset --hard HEAD` (discard) + return `CodeCherryPickConflict` ("pick the whole commit to resolve"). `git diff --cached --quiet` guards the "selected files have no change" case. Registered in `main.go`. Smoke-tested (kept f1 only; f2 reverted, f3 added-file dropped; message reused via `-C`).
- **Frontend** (`CommitFiles.svelte`): per-file checkbox (`.cb-wrap` label; row onclick ignores clicks inside it so ticking ≠ opening diff) + footer with "Select all" + "Pick N files → \<target\>" button (disabled when 0 selected or ALL selected — all-files = use normal Apply). New props `target` / `picking` / `onpartialpick`. `+page.svelte`: `partialPicking` state + `partialPick(keep)` handler — wraps `maybeAutoStash()`/`maybePopAutoStash()` (M11a) around `rpc.git.partialPick(repo, targetBranch, selectedCommit.sha, keep)`, then refreshes branches + applied set. Wired to `CommitFilesPanel`.
- **MCP**: partial pick is desktop-only — NOT exposed as an MCP tool (no AI use case for sub-commit file selection; documented choice per the npm/MCP-doc-sync rule). `mcp-npm` unchanged.
- Verified: `go vet ./...` clean + smoke test, `npm run check` 0 errors (58 warnings, unchanged after an a11y cleanup). **M11 complete → next is M12 (Performance).**

**M14f done** (Accounts tab: multi-repo connection management + Test-connection — per user request): **Rust command + frontend only, no sidecar/Go change.**
- **Rust** (`lib.rs`): new `forge_test_saved_connection(repo_path)` — loads the connection metadata + token from the keychain (token never reaches the frontend) and re-runs `provider.test_connection`, so the user can verify a stored token still authenticates after sitting unused. Mirrors the `forge_list_prs`/`forge_create_pr` token-load pattern. Registered in `invoke_handler`. `rpc.ts`: `forge.testSavedConnection(repoPath)`.
- **Settings → Accounts redesign** (`Settings.svelte`): was single-current-repo only → now **lists ALL connected repos** (read directly from `settings.forgeConnections`, which already holds every repo's metadata) as cards — repo name (+ "current" badge), forge/host/username, and per-card **Test / Reconnect / Disconnect**. Test result shows inline (⏳/✓ user·scopes /✗ error). Below: a **"Connect a repo" dropdown** of recent repos not yet connected (+ the current repo if unconnected) → Connect. New props `currentRepo`, `recents`, and callbacks now take a `repoPath`: `onconnectforge(path)` / `ondisconnectforge(path)` / `ontestforge(path)`.
- **`+page.svelte`**: `openConnectForge(path)` reads that repo's remotes (`git.remotes(path)` when `path !== repoPath`, else reuse current `remotes`) into `connectForgeRepo`/`connectForgeRemotes` so **ConnectForge can target a repo that isn't currently open**. `disconnectForge(path)` + `refreshForgeConnection()` (now reloads full `settings` even when no repo is open, so the live Accounts list updates). `ConnectForge` is wired to `connectForgeRepo`/`connectForgeRemotes` instead of the open repo.
- **UX — ConnectForge overlays ON TOP of Settings** (not replacing it): `openConnectForge` no longer sets `settingsOpen = false`, and ConnectForge's overlay z-index is bumped 500 → **600** (above Settings' 500). Closing ConnectForge (Cancel/✕/outside-click, or auto-close after Save → `onsaved()`+`onclose()`) returns to the still-open Settings, whose Accounts list updates live via the `$derived` over `settings.forgeConnections`.
- **Model unchanged**: still ONE `ForgeConnection` per repo (`HashMap<repo_path, …>`). Multi-forge-per-repo (create PR on several forges, pick forge per remote, + warn on remote/forge mismatch) is deferred to **M14e** (user decision 2026-06-14).
- Verified: `cargo build`, `npm run check` 0 errors. Runtime end-to-end is the user's next step.

**Not done**: Windows Authenticode signing (SmartScreen) — requires EV certificate, not planned. **M14e** (multi-forge per repo) — deferred.

See README roadmap for M5+ scope.

## Tech stack pins (as installed and verified)

- Node 22.17.0, npm 10.9.2
- Rust 1.95.0 stable (`x86_64-pc-windows-msvc`)
- Go 1.26.3
- Git 2.50 (Git for Windows)
- Tauri 2.11, `tauri-plugin-shell` 2.3, `tauri-plugin-opener` 2.5, `tauri-plugin-dialog` 2.x
- Svelte 5.x, SvelteKit 2.9, Vite 6.4
- MSVC Build Tools 2022 at `C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools`

## Cross-session memory

When working with the user on this repo, prior decisions are stored as Claude memories:
- `user-language-vi` — communicate in Vietnamese, prefer short option-style questions
- `project-m4-complete`, `project-m5-complete`, `project-m5c-wip`, `project-m5d-complete`, `project-m6-complete`, `project-m7-complete`, `project-m8-complete` — feature checklists per milestone
- `design-conflict-merge-logic` — conflict merge editor (provisional model, parser, render pipeline)
- `design-side-by-side-diff` — 2-panel diff viewer logic incl. trailing-newline transform with lookahead

These memories complement (not replace) this file. This file describes the **codebase**; memories describe the **user and decisions**.

## When stuck

- Sidecar weirdness → run it manually with a piped JSON request to isolate from Tauri (rule #5 may apply if first response is a parse error).
- Rust compile errors after editing `lib.rs` → check plugin version in `Cargo.toml` and the plugin docs.
- Tauri window blank → check the Vite log in the terminal where `tauri dev` runs before assuming Rust is broken.
- "Method not found" with correct method name → did you rebuild the sidecar binary? Rule #2.
- Frontend type error after adding a new RPC method → add types to `rpc-types.ts` and wrapper to `rpc.ts` first.
